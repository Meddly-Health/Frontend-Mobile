import 'package:flutter/material.dart';

const defaultPadding = EdgeInsets.all(16);
const defaultHorizontalPadding = EdgeInsets.symmetric(horizontal: 16);
const defaultVerticalPadding = EdgeInsets.symmetric(vertical: 16);
const baseUrl = 'http://54.196.11.43:8000';

const lightSkin = Color(0xffFFD4B3);
const mediumSkin = Color(0xffD39C72);
const darkSkin = Color(0xff532E15);

const brunette = Color(0xff0A0503);
const brown = Color(0xffAA5509);
const blonde = Color(0xffF6B704);
